const battleShipDisplay = require('./battleshipdisplay');

test('returns HTML', () => {
  expect(battleShipDisplay()).toBeTruthy();
});
test('returns HTML', () => {
  expect(battleShipDisplay()).toBeTruthy();
});
